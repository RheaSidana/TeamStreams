CREATE TABLE channel (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
    OWNER_ID BIGINT REFERENCES ts_user (ID),
    NAME VARCHAR(255) NOT NULL,
    DESCRIPTION VARCHAR(1000) NOT NULL,
    PRIMARY KEY (ID)
);
